services:
  frontend:
    image: morse-vue
    build: vue-project/Dockerfile
    ports:
    - "5173:5173"

  backend:
    image: morse-backend
    build: backend/Dockerfile
    ports:
    - "8080:8080"
